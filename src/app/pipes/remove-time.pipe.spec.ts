import { RemoveTimePipe } from './remove-time.pipe';

describe('RemoveTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveTimePipe();
    expect(pipe).toBeTruthy();
  });
});
